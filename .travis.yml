sudo: required

services:
  - docker

env:
  global:
    - appUser="media"
    - appGroup="1000"

install:
  - docker build --rm --no-cache -t scorputty/sabnzbd ../sabnzbd

script:
  - ./rundocker.sh
  - sleep 10
  - bats tests/

notifications:
  email: false
